import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Spade & Scale — Business Intelligence Dashboard",
  description:
    "AI-powered competitor analysis, pricing intelligence, and profit forecasting for SMEs.",
  keywords: ["business intelligence", "competitor analysis", "pricing", "profit", "SME"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
